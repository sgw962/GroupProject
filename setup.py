from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Project containing the code for creating a model to predict next day closing stock price',
    author='Sean',
    license='',
)
